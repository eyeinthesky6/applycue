# Architecture pointer

The canonical ApplyCue architecture is [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

One runtime, one tracker, one browser dashboard. Deterministic code owns objective work; the external agent owns semantic judgment and writing.
