# Legacy Gemini CLI context

Use `AGENTS.md` and the ApplyCue skill entrypoint instead:

```text
skills/applycue/SKILL.md
```

This file intentionally stays small so newer agent CLIs do not load duplicate long instructions.
